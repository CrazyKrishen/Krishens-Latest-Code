//converting strings into float, double, int, or long, respectively
#include <iostream>
#include <cstring>
using namespace std;

int main()
{
	
	//note how to convert string into number:
	//Use stof, stod, stoi, or stol to convert a string to a float, double, int, or long, respectively.
	//int i
	//double d
	//string s
	
	//i = stoi("35"); // Converts the string "35" to an integer 35
	//d = stod("2.5"); // Converts the string "2.5" to the double 2.5
	//s = to_string(d*2); // Converts the double 5.0 to a string "5.0000"
	
	return 0;
}
